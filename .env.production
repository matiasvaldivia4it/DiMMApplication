# Production Environment Variables

# Domain Configuration
# Domain Configuration
REACT_APP_API_GATEWAY=https://api.dimma.auditoriasenlinea.com.ar
GOOGLE_CALLBACK_URL=https://api.dimma.auditoriasenlinea.com.ar/api/auth/google/callback

# Google OAuth Configuration
# Get these from https://console.cloud.google.com/apis/credentials
# IMPORTANT: Update authorized redirect URIs in Google Cloud Console
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Google AI API Configuration (Gemini)
# Get this from https://aistudio.google.com/app/apikey
GOOGLE_AI_API_KEY=your_google_ai_api_key_here

# JWT Secret (generate a random string)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_minimum_64_characters_here

# VAPID Keys for Push Notifications
# Generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your_vapid_public_key_here
VAPID_PRIVATE_KEY=your_vapid_private_key_here

# Database Configuration (default values, change if needed)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_this_in_production
POSTGRES_DB=postgres

# MinIO Configuration (change in production)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=change_this_in_production

# Node Environment
NODE_ENV=production
